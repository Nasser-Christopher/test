/*
 * @param {number[]} nums
 * @return {number[]}
 */
var productExceptSelf = function(nums) {
    let res = Array.from(nums);

    for (let index = 0; index < nums.length; index++)
    {
        
    }
    
};